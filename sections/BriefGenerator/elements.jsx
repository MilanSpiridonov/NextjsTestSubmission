import styled from "styled-components";
import { SectionBigHeading } from "~/components";
export const StyledTitle = styled((props) => <SectionBigHeading {...props} />)`
  margin: 0;
  color: black;
  text-align:center;
`;